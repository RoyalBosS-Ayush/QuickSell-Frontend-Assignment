import React from 'react';
import './header.css'
import DisplayDropdown from '../Dropdowns/DisplayDropdown';

function Header() {

    return (
        <header>
            <DisplayDropdown />
        </header>
    );
}

export default Header;
